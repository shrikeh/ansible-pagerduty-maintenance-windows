{
  "maintenance_window": {
    "start_time": {{ item.1.start | to_json }},
    "end_time": {{ item.1.end | to_json }},
    "description":  {{ item.0.description | to_json }},
    "service_ids": {{ item.0.service_ids | to_json }}
  }
{% if item.0.requester_id is defined %}
  ,
  "requester_id": {{ item.0.requester_id | to_json }}
{% endif %}
}
